# GENERAL SETTINGS
# Set DEBUG = False for production!
DEBUG=True
# Generate a strong, unique secret key for production using e.g., python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY='django-insecure-your-development-key-here' # CHANGE THIS FOR PRODUCTION
# Comma-separated list of allowed hosts/domains for the backend
ALLOWED_HOSTS=127.0.0.1,localhost

# DATABASE
# Development (SQLite) - Default if DATABASE_URL is not set
# DATABASE_URL=sqlite:///src/api/db.sqlite3
# Production (PostgreSQL Example - Replace with your actual URL)
# DATABASE_URL=postgres://YOUR_USER:YOUR_PASSWORD@YOUR_DB_HOST:5432/YOUR_DB_NAME

# CORS - Configure for your frontend domain in production
# Development (Allows Vite default port and common alternatives)
CORS_ALLOWED_ORIGINS=http://localhost:9000,http://127.0.0.1:9000,http://localhost:5173,http://127.0.0.1:5173
# Production Example (Replace with your frontend domain)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# EMAIL (Configure for production password resets, etc.)
# Development (Prints emails to console)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Production (Example using SendGrid - replace with your provider/settings)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey # Often 'apikey' for SendGrid
# EMAIL_HOST_PASSWORD=YOUR_SENDGRID_API_KEY
# DEFAULT_FROM_EMAIL=Your Name <noreply@yourdomain.com>

# DJANGO settings module (Usually not changed)
DJANGO_SETTINGS_MODULE=quillpad_backend.settings
PYTHONPATH=./src/api

# VITE proxy target (Used in vite.config.js explanation, not directly by Django)
# VITE_API_TARGET=http://localhost:8000